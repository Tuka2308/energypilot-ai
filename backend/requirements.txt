fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.9

# OCR загрузки счёта (app/services/bills_service.py). pytesseract — обёртка
# над системным бинарником tesseract-ocr (см. Dockerfile/README — его нужно
# ставить отдельно, pip его не тянет). pymupdf — рендер первой страницы PDF
# в изображение перед OCR, чистый wheel без системных зависимостей.
pytesseract==0.3.13
pillow==12.3.0
pymupdf==1.28.0

# SQLAlchemy + Postgres-драйвер добавятся, когда роуты начнут реально
# писать в БД (сейчас сервисы — моки, зависимость была бы неиспользуемой).

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.9

# Распознавание счёта (app/services/bills_service.py). pymupdf — читает
# текстовый слой и таблицы цифровых PDF (основной путь для квитанций ЭСО),
# а также рендерит страницу в изображение для сканов. pytesseract — OCR
# фото/сканов, обёртка над системным бинарником tesseract-ocr (см.
# Dockerfile/README — pip его не тянет, ставится отдельно).
pytesseract==0.3.13
pillow==12.3.0
pymupdf==1.28.0

# Прогноз счёта — ОДНА модель Prophet (без ансамбля, см. MVP-скоуп
# CLAUDE.md). prophet тянет cmdstanpy, который при первом использовании
# компилирует CmdStan (C++) — в Docker это делается на этапе сборки образа
# (см. Dockerfile), локально прогревается один раз. pandas — подготовка
# ряда для Prophet, импортируется в forecast_service напрямую.
prophet==1.3.0
pandas==3.0.3

# AI-чат энергокоуч (app/services/chat_service.py): каскад провайдеров.
# Реальный порядок в _call_llm — OpenAI → Gemini → Ollama → офлайн-fallback
# (OpenAI первым: Gemini free tier у команды недоступен по региону, см.
# backend/README.md). openai — основной путь, google-genai — запасной.
# Офлайн-fallback'ам SDK не нужен: Ollama вызывается через stdlib urllib,
# детерминированный fallback — чистый Python.
google-genai==2.12.1
openai==2.46.0

# Персистентность в PostgreSQL (app/db/*). psycopg[binary] — драйвер v3
# (psycopg2-binary НЕ ставится под локальный Python 3.14, проверено);
# binary-wheel есть и для 3.14 (venv), и для 3.12-slim (контейнер).
# SQLAlchemy 2.0.51: более ранние 2.0.x падают на разборе аннотаций
# Mapped[X | None] под Python 3.14 (в контейнере 3.12 работали и они, но
# держим одну версию для обоих окружений).
sqlalchemy==2.0.51
psycopg[binary]==3.2.10

# Тесты (распознавание счёта, прогноз, аномалии, чат — на изолированной
# тестовой БД PostgreSQL, см. tests/conftest.py).
pytest==8.3.3
